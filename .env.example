# 실제 값은 Cloudflare Dashboard에서 설정하세요
# 이 파일은 템플릿용이며, 실제 비밀 정보를 입력하지 마세요

# bcrypt로 해시된 관리자 비밀번호
# 생성 방법: node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('YOUR_PASSWORD', 10));"
ADMIN_PASSWORD_HASH=<bcrypt_hash_here>

# JWT 서명용 시크릿 (최소 32자 이상의 랜덤 문자열)
JWT_SECRET=<random_secret_here>
